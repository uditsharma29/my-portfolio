import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Udit Sharma - Portfolio | Machine Learning Engineer",
  description: "Portfolio of Udit Sharma, a Machine Learning Engineer showcasing projects in data science, AI, and software development.",
  keywords: ["Udit Sharma", "Portfolio", "Machine Learning", "AI", "Data Science", "Software Engineer", "GitHub Projects"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
